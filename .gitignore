# Artifacts
*.db
*.sqlite
*.zip

# Planning docs (keep on Drive/Notion instead of repo)
*.docx
*.pptx

# Rust
target/
**/*.rs.bk

# Env
.env
.env.*
!.env.example

# Grafana dashboards (volume is excluded, JSON files are tracked)
# grafana/dashboards/*.json

# Scratch
scratch/
**/*.zip
**/*.log
**/*.tar.gz